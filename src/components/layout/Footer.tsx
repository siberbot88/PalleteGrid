export function Footer() {
  return (
    <footer className="mt-20 flex flex-wrap items-center justify-between gap-4 border-t border-black/5 py-8 text-sm text-[var(--app-muted)]">
      <p>PaletteGrid builds color, type, preview, and export tokens locally.</p>
      <p>No login. No database. No external AI.</p>
    </footer>
  );
}
